---
 - name: Checking Kernel Version
   hosts: lab
   tasks:
     - name: server kernel
       command: uname -a
       register: kernel
     - debug:
          msg: "kernel Version Server is {{kernel}}"
