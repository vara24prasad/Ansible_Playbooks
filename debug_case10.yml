---
 - name: Debug Module for case9
   hosts: lab
   vars:
     os: "linux"
     cloud: "aws"
     tools: "devops"
   tasks:
     - debug:
         msg: "I love {{os}}"
     - debug:
         msg: "I choose {{cloud}}"
     - debug:
         msg: "I like {{tools}}"
